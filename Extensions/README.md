# Extensions

This directory is reserved for future Xcode extension targets.

The first planned extension is `SwiftMonocleXcodeExtension`, an Xcode Source Editor Extension for command-first editor integration. Do not add extension targets as a side effect of package or app-shell work; introduce them through an explicit roadmap item and XcodeGen spec update.
